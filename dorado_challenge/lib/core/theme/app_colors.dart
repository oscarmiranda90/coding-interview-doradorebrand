import 'package:flutter/material.dart';

abstract final class AppColors {
  /// Neon yellow — primary accent (CTA button, card shadow, logo)
  static const Color yellow = Color(0xFFE9FF47);

  /// Near-black — borders, text, shadows
  static const Color black = Color(0xFF0A0A0A);

  /// Warm off-white — card background, light text
  static const Color offWhite = Color(0xFFFAFAF5);

  /// Screen background
  static const Color darkBg = Color(0xFF0E0E0E);

  /// Input / bar background
  static const Color inputBg = Color(0xFFF5F5EE);

  /// Confirmation green
  static const Color successGreen = Color(0xFF4ADE80);

  /// Loading grey
  static const Color loadingGrey = Color(0xFF555555);
}
